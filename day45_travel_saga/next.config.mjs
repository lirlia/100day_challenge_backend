/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // ポート番号を3001に設定 (npm run dev で -p オプションを使わない場合)
  // devServer: {
  //   port: 3001,
  // },
  // experimental: {
  //   serverActions: true, // Server Actions を有効にする場合
  // },
};

export default nextConfig;
