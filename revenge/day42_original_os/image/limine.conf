timeout: 5

/KERNEL
    protocol: limine
    # Request x2APIC mode for the kernel (if available)
    FEATURE=x2apic
    # Assuming limine.conf is in /boot/limine/ and kernel is in /boot/
    path: boot():/boot/kernel.elf
